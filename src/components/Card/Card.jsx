import React from "react";

const Card = ({ brand, title, rating, price, description, image }) => {
  return <div>
    <div>
        <img src="" alt="" />
    </div>
    <div>
        
    </div>
  </div>;
};

export default Card;
